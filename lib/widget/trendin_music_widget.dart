import 'package:flutter/material.dart';

class TrendingMusicWidget extends StatelessWidget {
  const TrendingMusicWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: BoxDecoration(),
    );
  }
}
